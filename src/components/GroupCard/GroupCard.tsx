import * as React from 'react';
import {formatISODateString} from "../../utils/dateUtils";

export const GroupCard = ({item}) => {
    return (
        <div className="ods_raidplanner_groupcard-container">

        </div>
    )
}